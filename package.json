{
  "name": "launch-it-desktop",
  "version": "1.0.0",
  "description": "An launcher app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package-win": "electron-packager . MyApp --platform=win32 --arch=x64 --out=dist --icon=assets/app-icon.ico --overwrite"
  },
  "author": "Ajil Jagadeesh",
  "license": "MIT",
  "devDependencies": {
    "electron": "^33.0.2",
    "electron-packager": "^17.1.2"
  }
}
